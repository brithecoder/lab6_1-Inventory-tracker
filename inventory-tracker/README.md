### Inventory-tracker 

This Lab utilizes Typescript to run lab cd into the src file then run the command 
(node ./main.js) you should see a console.log of the items that were created and added in the productList array.